---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "私について";

const identity = {
  firstName: "サラ",
  country: "カナダ",
  occupation: "技術ライター",
  hobbies: ["写真", "バードウォッチング", "野球"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "ドキュメントの執筆"];

const happy = false;
const finished = false;
const goal = 3;
---

<BaseLayout title={pageTitle}>

  <h1 class="text-3xl font-bold mb-4">{pageTitle}</h1>
  <h2 class="text-xl text-gray-700 mb-6">... そして私の新しいAstroサイトについて！</h2>

  <p class="mb-4">私はAstroの入門チュートリアルを進めています。これは私のウェブサイトの2番目のページで、自分で作った初めてのページです！</p>

  <p class="mb-4">このサイトはチュートリアルを進めるにつれて更新されていきます。定期的にチェックして、私の旅の様子を見に来てください！</p>

  <p class="mb-2 font-semibold">以下は私に関するいくつかの事実です。</p>
  <ul class="list-disc list-inside mb-6 space-y-1">
    <li>私の名前は{identity.firstName}です。</li>
    <li>{identity.country}に住んでおり、{identity.occupation}として働いています。</li>
    {identity.hobbies.length >= 2 && (
      <li>私の2つの趣味は{identity.hobbies[0]}と{identity.hobbies[1]}です。</li>
    )}
  </ul>

  <p class="mb-2 font-semibold">私のスキルは以下の通りです。</p>
  <ul class="list-disc list-inside mb-6 space-y-1">
    {skills.map((skill) => (
      <li>{skill}</li>
    ))}
  </ul>

  {happy && (
    <p class="mb-4 text-green-600">I am happy to be learning Astro! Astroを学べて幸せです！</p>
  )}

  {finished && (
    <p class="mb-4 text-blue-600">I finished this tutorial! このチュートリアルを終えました！</p>
  )}

  {goal === 3 ? (
    <p class="text-purple-600 font-medium">My goal is to finish in 3 days. 3日で終えるのが目標です。</p>
  ) : (
    <p class="text-gray-500">My goal is not 3 days. 3日が目標ではありません。</p>
  )}

</BaseLayout>

